# Functional-Programming
Es6

-Write a function with two parameters (keys and values) as an arrays
-Associate the keys and values as objects

function associativeArray(keys,values){
   //loop through the keys
   //get each element of the keys and values
   let obj= {};
   for(let i=0; i< keys.length; i++){
     let key= keys[i];
     let value= values[i];
     obj[key]= value;
   }
   return obj;
   
  //return the key of max value
  
}


console.log(associativeArray(['kiwi','mango','laranja'],[20,4,60]));
